<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caelan.mapper.CUserMapper">
    <!--更新密码-->
    <update id="updatePwd" parameterType="com.caelan.common.dto.updatePwddto">
        update c_user set password=#{newpassword,jdbcType=VARCHAR} where username=#{username,jdbcType=VARCHAR}
    </update>

    <!--识别码-->
    <update id="updateCode" parameterType="com.caelan.entity.CUser">
        update c_user set code=#{code,jdbcType=VARCHAR} where username=#{username,jdbcType=VARCHAR}
    </update>
</mapper>
